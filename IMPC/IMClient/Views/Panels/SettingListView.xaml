<UserControl x:Class="IMClient.Views.Panels.SettingListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IMClient.Views.Panels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <DataTemplate x:Key="DataTemplate">
            <ContentControl>
                <Border x:Name="border" Cursor="Hand" Background="Transparent" Height="64" MouseLeftButtonDown="border_MouseLeftButtonDown">
                    <TextBlock Text="{Binding Model.Name}" FontSize="18" VerticalAlignment="Center" Foreground="#4E546A" Margin="30,0,0,0"/>
                </Border>
            </ContentControl>
        </DataTemplate>
    </UserControl.Resources>

    <ListBox x:Name="list" ItemsSource="{Binding Items,Mode=OneWay}" SelectedItem="{Binding SelectedItem,Mode=TwoWay}" Margin="0,60,0,0"
                 ItemTemplate="{StaticResource DataTemplate}">
    </ListBox>
</UserControl>
